export function SiteFooter() {
  return (
    <footer className="border-t border-border/60 mt-32">
      <div className="max-w-6xl mx-auto px-6 md:px-10 py-8 flex flex-col md:flex-row justify-between gap-4 font-mono text-[10px] text-muted-foreground uppercase tracking-widest">
        <div>(a) GitWhisper Core v0.1 — RAG over GitHub</div>
        <div>(b) Built with Lovable AI · Hybrid Search · Citations</div>
      </div>
    </footer>
  );
}
